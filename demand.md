checklist:
 - 1. 增加一个参数，调整可视化中的机器人编号大小
 - 2. 增加交互，在可视化中选中机器人，要显示正在处理的订单编号、订单所需skus等信息
 - 3. 增加交互，在可视化中选择pod，要显示该pod中实时的sku数量、类型信息
 - 4. 在ui的左侧信息区域，在info_box下方增加一个展示机器人路径的信息区，展示当前每个机器人携带的pod编号、(pod的起始位置->工作站位置->pod要被放回的位置)
 - 5. Panda3d 这个库能实现3d可视化里面的，对由立方体表示的小车进行贴图吗？


pay attention:
 - 1. 用 conda activate multi_robot 激活测试环境